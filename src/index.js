import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

// Redo the useReducer on the count app, then maybe attempt the todo.
// Don't let the Todo element bother you too much though.
